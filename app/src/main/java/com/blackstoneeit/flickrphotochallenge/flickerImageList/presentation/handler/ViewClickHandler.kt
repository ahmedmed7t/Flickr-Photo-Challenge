package com.blackstoneeit.flickrphotochallenge.flickerImageList.presentation.handler

import android.view.View

interface ViewClickHandler {
    fun onGlobalFilterClick(view: View)
    fun onLocalFilterClick(view: View)
}